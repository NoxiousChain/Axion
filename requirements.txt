# Core
pyyaml==6.0.3
numpy==2.0.2
pandas==2.3.3
tabulate==0.9.0
requests==2.33.0

# API server
fastapi==0.128.8
uvicorn[standard]==0.39.0
pydantic==2.13.2
PyJWT==2.13.0
starlette==1.0.1

# Observability
prometheus-fastapi-instrumentator==7.0.2
python-json-logger==3.3.0

# Packet capture (optional — requires root for live mode)
scapy==2.7.0

# SQLCipher — DB-at-rest encryption.
# macOS (no Homebrew): built from source into vendor/sqlcipher/ — see scripts/build_sqlcipher.sh
# Alpine: apk add sqlcipher-dev  |  Debian: apt install libsqlcipher-dev
# Without it the server falls back to unencrypted SQLite with a WARNING.
sqlcipher3==0.5.3

# MFA — TOTP second-factor for privileged accounts (IA-2(1)).
pyotp==2.9.0

# Testing / dev
pytest==9.0.3
httpx==0.28.1
pip-audit==2.9.0
